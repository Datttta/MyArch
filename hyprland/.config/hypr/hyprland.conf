################
### MONITORS ###
################

source = /home/DROS/.cache/wal/colors-hyprland.conf

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

$terminal = kitty
$fileManager = kitty yazi
$menu = wofi --show drun --normal-window #wofi --show drun --normal-window


#################
### AUTOSTART ###
#################

exec-once = ~/dotfiles/scripts/random-wallpaper.sh   
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = syncthing --no-browser
exec-once = copyq --start-server
exec-once = [workspace 2 silent] kitty -e nvim +VimwikiIndex
exec-once = [workspace 5 silent] kitty -e bluetoothctl
exec-once = [workspace 5 silent] blueman-manager

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = DRI_PRIME,0
env = EDITOR,nvim      
env = SHELL,/usr/bin/zsh
env = LIBVA_DRIVER_NAME,iHD
env = VDPAU_DRIVER,va_glenv = LIBVA_DRIVER_NAME,iHD
env = VDPAU_DRIVER,va_gl
env = QT_QPA_PLATFORM, xcb
env = QT_QPA_PLATFORM,wayland
env = QT_QPA_PLATFORMTHEME,qt6ct

###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

 permission = /usr/(bin|local/bin)/grim, screencopy, allow
 permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
 permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

debug {

    disable_logs = false

    # never display config‑parsing errors on screen
    suppress_errors = false

}

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    
    gaps_in = 4
    gaps_out = 4

    border_size = 1 #1.5 default

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = $color14 # $color2 or $color3
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle #you can use master too 
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {

    rounding = 4 #more squared: 3
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    #chnage both opacity to 2 to make borders thicker and brighter
    active_opacity = 1
    inactive_opacity = 1

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 1
        passes = 4
        noise = 0.01

        xray = false
        new_optimizations = true
        popups = false
    }
}


# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    #force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    #disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = br
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    accel_profile = flat
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = ALT # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, X, exec, hyprctl killactive && kill -9 $(hyprctl activewindow -j | jq -r '.pid')
bind = $mainMod CTRL, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, SPACE, togglesplit, # dwindle
bind = $mainMod, F, fullscreen,

#wlogout
bind = $mainMod SHIFT, L, exec, wlogout

#Swaync client
bind = $mainMod, N, exec, swaync-client -t

#wofi clipboard
bind = $mainMod SHIFT, C, exec, copyq toggle

#Switching wallpaper
bind = $mainMod, W, exec, waypaper

#screenshot
bind = CTRL SHIFT, S, exec, ~/dotfiles/scripts/screenshot.sh
bind = $shiftMod, PRINT, exec, grimblast save screen

#Waybar restart
bind = $mainMod SHIFT, R, exec, ~/dotfiles/scripts/waybarRestart.sh

#Swaync restart
bind = $mainMod SHIFT, N, exec, ~/dotfiles/scripts/swayncRestart.sh

#volume
bind = $mainMod, EQUAL, exec, pamixer -i 4
bind = $mainMod, MINUS, exec, pamixer -d 4

#brightness
bind = $mainMod, F12, exec, brightnessctl set +10% 
bind = $mainMod, F11, exec, brightnessctl set 10%-

#Bluetooth
bind = $mainMod, F1, exec, ~/dotfiles/scripts/connectBluetoothEarbuds.sh
bind = $mainMod, F2, exec, ~/dotfiles/scripts/disconnectBlutooth.sh
bind = $mainMod, F3, exec, ~/dotfiles/scripts/fastBluetooth.sh
bind = $mainMod, F4, exec, ~/dotfiles/scripts/highQBluetooth.sh

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# No blur
windowrule = match:class kitty, no_blur on

windowrule = match:class easyeffects|pavucontrol, no_blur on 

windowrule = match:class Xdg-desktop-portal-gtk, no_blur on
windowrule = match:class xdg-desktop-portal-gtk, no_blur on
windowrule = match:class Xdg-desktop-portal-hyprland, no_blur on
windowrule = match:class xdg-desktop-portal-hyprland, no_blur on

windowrule = match:title (Open File|Open|Save|Save As|Export|Import|Choose File), no_blur on

# Ignore maximize requests from apps.
windowrule = match:class Vivaldi-stable|Chromium|Midori|firefox|Opera, suppress_event fullscreen maximize

windowrule = match:class Opera, size 100% 100%

# Make windows floating
windowrule = match:title .*(Open File|Open|Save|Save As|Export|Import|Choose File|Rename).*, float on
windowrule = match:class (firefox|Opera|Vivaldi-stable), float on

windowrule = match:class Xdg-desktop-portal-gtk, float on
windowrule = match:class xdg-desktop-portal-gtk, float on
windowrule = match:class Xdg-desktop-portal-hyprland, float on
windowrule = match:class xdg-desktop-portal-hyprland, float on

windowrule = match:class waypaper, float on

# No border
#windowrule = match:class (Xdg-desktop-portal-gtk|Vivaldi-stable|Opera), border_size 0

# Change windows size
windowrule = match:class waypaper, size 900 550

# Prevent blury borders on chromium browsers
windowrule = match:class ^$, match:title ^$, no_blur on

# Wofi
windowrule = match:class wofi, rounding 10

# Copyq
windowrule = match:class com.github.hluk.copyq, float on

# Fix some dragging issues with XWayland
windowrule {
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Workspaces
#workspace = 1, gapsout:0

## layerrules
layerrule = match:namespace waybar|swaync-control-center|logout_dialog, blur 1
layerrule = match:namespace waybar|swaync-notification-window|swaync-control-center, ignore_alpha 0

